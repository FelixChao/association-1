<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.lvjp.association.mapper.AssociationMapper">

    <select id="selectById" parameterType="Integer" resultType="associationInfo">
        select association_id,association_name,association_description,association_icon from association where association_id = #{id}
    </select>

    <select id="selectPop" parameterType="Integer" resultType="associationInfo">
        select association_id,association_name,association_icon from association order by apply_number limit #{count}
    </select>

    <select id="selectByCategory" parameterType="String" resultType="associationInfo">
        select association_id,association_name,association_icon from association where association_category = #{category}
    </select>

</mapper>