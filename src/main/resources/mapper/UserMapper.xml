<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.lvjp.association.mapper.UserMapper">

    <!--<resultMap id="user" type="top.lvjp.association.entity.User">-->
        <!--<id column="user_id" property="userId" />-->
        <!--<result column="user_name" property="userName" />-->
        <!--<result column="user_password" property="userPassword" />-->
        <!--<result column="user_type" property="userType" />-->
    <!--</resultMap>-->

    <select id="selectByNameAndPassword" resultType="user">
        select user_id,user_name,user_password,user_type
        from user
        where user_name = #{name} and user_password = #{password}
    </select>

    <select id="selectById" parameterType="integer" resultType="user">
        select user_id,user_name,user_password,user_type
        from user
        where user_id = #{id}
    </select>

    <select id="selectAll" resultType="user" >
        select user_id,user_name,user_password,user_type
        from user
    </select>

    <insert id="insert" parameterType="top.lvjp.association.entity.User">
        insert user (user_name,user_password,user_type)
        values (#{userName},#{userPassword},#{userType})
    </insert>

    <update id="update" parameterType="top.lvjp.association.entity.User" >
        update user
        set user_name = #{userName},user_password = #{userPassword},user_type = #{userType}
        where user_id = #{userId}
    </update>

    <delete id="deleteById" parameterType="integer">
        delete from user
        where user_id = #{id}
    </delete>
</mapper>